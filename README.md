# AI PDF PARSING

## Setup

```bash
uv init

uv add google-genai # to ask to gemini ai
uv add pydantic     # to get stuctured output from ai 
uv add pymupdf      # to edit/modify pdf
```

`.bashrc`
```
GEMINI_API_KEY="alksjdbvo9ah" # at end of bashrc file, append this line
```